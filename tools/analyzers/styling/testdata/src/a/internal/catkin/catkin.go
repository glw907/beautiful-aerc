// Package catkin is the live-markdown editor; the styling boundary
// excludes it too.
package catkin

import "lipgloss"

// Arrow is a glyph the editor renders directly.
const Arrow = "→"

// Base is the editor's base style.
var Base = lipgloss.NewStyle()
