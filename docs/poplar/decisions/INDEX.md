# Decision index

Themed map of poplar's ADRs back to the binding facts they justify.
Numbering is chronological. Load this when chasing rationale by
theme; load a specific ADR file under `docs/poplar/decisions/`
when you need the full record.

| Invariant theme | ADRs |
|---|---|
| Monorepo, single binary | 0001, 0058 |
| Direct-on-libraries mail stack (no aerc fork) | 0002, 0006, 0008, 0010, 0012 (all superseded by 0075), 0075 |
| Lipgloss + compiled themes, styling discipline | 0004, 0043, 0046 |
| JMAP + IMAP only, minimal account config | 0009, 0075, 0098, 0101, 0104 |
| Mail backend interface synchronous | 0010 (superseded by 0075), 0075, 0099 |
| Config layout, folder classifier, UI config | 0013, 0052, 0053, 0102, 0103 |
| Elm architecture in internal/ui/ | 0023, 0035, 0036, 0037, 0042, 0044, 0054, 0088 |
| Frame, chrome, status, footer | 0025, 0026, 0027, 0028, 0029, 0030, 0038 |
| Sidebar groups, nested indent, classification | 0018, 0019, 0034, 0049, 0050 |
| Sidebar tree, expand/collapse, depth cap | 0198 |
| Message list, threading, fold | 0041, 0045, 0047, 0048, 0055, 0059, 0060, 0061, 0062, 0063, 0199 |
| Vim-first keybindings — no command mode, no multi-key, no modifiers (text-entry exempt per 0076) | 0015, 0024, 0051, 0068, 0076 |
| Compose, Catkin, editor interface, library foundation | 0031, 0032, 0033, 0076 |
| Per-screen prototype passes | 0022 (superseded by 0070), 0070 |
| Sidebar search shelf, filter-and-hide, thread-level | 0064 |
| Viewer prototype, footnote harvesting, optimistic mark-read, n/N nav, long-bare-URL footnoting | 0065, 0066, 0067, 0069, 0085, 0086 |
| List-Unsubscribe (RFC 8058 one-click) | 0185 |
| `.ics` invite viewer — display-only inline block (`internal/icalendar/` wraps `arran4/golang-ical`; first-VEVENT-only; aerc-aligned plain-text block; `[CANCELLED]` row on METHOD=CANCEL; FREQ+INTERVAL recurrence only; local-tz formatting; no RSVP / CalDAV / popover) | 0186 |
| Background body sync — per-account `Backfiller` (newest-first SQL queue, 500ms tick, 2 MB batch, 5s idle gate on `tea.KeyMsg`, online/cap gates, throttle backoff via local `expBackoff` helper); `[cache] max-size = 0` default (unlimited; partially supersedes 0122); status-bar `↓ N/M` segment with paused/warn substates and Spartan-tier glyph collapse; App `refreshBackfillSegment` with change-detection guard | 0187 |
| Search — `internal/search` operator parser; schema v11 `messages_fts` FTS5 (regular, rowid = `messages.id`); `internal/cache/fts.go` write helpers inside upsert+storeBody tx; `(*Account).Search` with cross-folder scope + `in:`/`has:attachment` SQL filters + `sent_at DESC`; `content.ExtractPlainText` shared MIME→text; sidebar `\` scope toggle (Tab retired); `[folder]`/`[all folders]` badge; `messagelist.SetSearchResults` with `[Folder] ` sender prefix; `BackfillProgress` warn flag wired to `↓ ⚠` substate (extends 0187) | 0188 |
| Help popover modal, future-binding policy, overlay+dim, link picker | 0071 (superseded by 0082), 0072, 0082, 0087, 0200, 0201, 0202 |
| Error banner, ErrorMsg, shared spinner | 0073, 0074 |
| Optimistic triage with toast/undo, ActionTargets, visual mode, move picker | 0089, 0090, 0091 |
| Permanent-delete primitive, retention sweep, manual empty + ConfirmModal | 0092, 0093, 0094, 0100 |
| Bubbletea conventions — research-grounded, lint hook, displayCells, key dispatch, WindowSizeMsg | 0077, 0078, 0079 (sup. 0084), 0080, 0081, 0083 (narrowed 0084) |
| Icon-mode policy: NF autodetect + CPR probe + simple/fancy tables | 0084 |
| Path-scoped UI rule (split from invariants) | 0095 |
| Responsive sidebar; 80×24 polish bar | 0096 (superseded by 0109), 0097, 0109 |
| Release model — pre-beta / beta soak / post-1.0 | 0105 |
| v0.9.0 README, license confirmation, and beta-soak tag | 0205 |
| Compose body focus regression — restore `editor.Focus()` in `setFocus(focusBody)` after Pass 9n collapsed it with `focusFrom` | 0206 |
| First-launch safety — `config.ResolvePreset` shared between TOML decoder and `wizard.Apply` so probe sees resolved Host/Source/SMTP (#49); MockBackend gated behind `//go:build dev`, `make test` uses `-tags=dev`, release binaries refuse `provider = "mock"` (#51); empty `provider` rejected explicitly; template Name-defaults-to-Email regression test (#29) | 0207 |
| Gmail preset, X-GM-EXT-1 assertion, Destroy routing, XOAUTH2 via password-cmd | 0106, 0107, 0108 |
| OAuth refresh for Gmail/Outlook IMAP — loopback PKCE + keyring/age-file token store; BYO client; `--reauth=<name>` | 0193 |
| Native OAuth as the canonical XOAUTH2 path — `mailimap.Probe`/`ProbeSMTP` accept `*mailauth.Client`, `wizard.Probe` threads it, probe stage builds the client from wizard state via `ui/wizard.buildOAuthClient`; `oauth2l` recipe dropped from the template | 0220 |
| Local cache architecture — per-account SQLite, typed Op sum + drainer, drain-first sync, outbox state machine, UIDVALIDITY re-key, IMAP scan-and-diff | 0110–0118, 0120–0124 |
| Backend error sentinels (mail.ErrAuth, mail.ErrNotFound, mail.ErrConnection) — typed at the protocol→cache boundary, attached via `mail.WrapSentinel`; drainer routes via errors.Is | 0119, 0208 |
| IMAP connection-dead recovery + outbox Append gate — `mail.ErrConnection` for cmd/idle redial (`b.dialFn` seam, `cmdClient`/`dialIdle`, `maybeDropOnConn`, idleLoop redial loop); `nextOutboxRow` NOT-EXISTS gate holds draft-linked Append until sibling Send hits OpDone; DiscardOp cascades to sibling Append | 0208 |
| Cache cutover + II policy — UI reads/writes via cache.Account; mail.Backend shrunk; MessageList Apply* removed; lazy body population with single max-size backstop, no LRU; FetchBody returns []byte; poplar cache CLI | 0121, 0122, 0123, 0124 |
| Pass 8.5 audits — overengineering trim (Backend/config/AccountConfig/cache.Cache/theme cleanup); Elm conformance (URLOpener seam, ConfirmModalYesMsg, OpenLinkPickerMsg, typed triageOp, key.Bindings, AccountTab.now); UI structural (`ModalShell`, `SidebarColumn`, `*<T>Cache` view-stable escape hatch); content/filter sealed-sum markers | 0125–0131 |
| Cache III — outbox visibility (Q/! overlays, RetryOp/DiscardOp + revert mirror, status-bar outbox depth segment, offline UI hint) | 0132, 0133, 0134 |
| Attachments — backend support (`mail.Attachment`, `Attachments`/`FetchAttachment`, schema v5, lazy metadata + bytes, `MaxAttachmentSize` backstop, JMAP/IMAP wiring, `mail.ClassifyDisposition`); viewer chip row + App-owned `AttachPicker` (`@` opens, `o`/Enter/digit open via `xdg-open`, `s` saves to `[ui] download_dir`); `internal/humanize` shared with cache CLI | 0135–0140 |
| Human-voice policy — `~/.claude/docs/go-comment-voice.md` (40-tell catalogue, voice palette, §0 write-time rubric + paraphrase test, §9b calibrated pairs); `go-conventions` carries it inline; `/simplify` runs the paraphrase test as a primary check and flags tells by number; grep-tier voice-check in `make check` covers T4, T10, T14, T16, T27, T28, T33, T35, T39 (narrow label-colon godoc), T40a (NOTE/IMPORTANT/TODO prefix), T41 (SPDX header — repo-level LICENSE is canonical, vendored files use prose provenance); T34 (semicolon clause-joiner) is voice-lens only; T38 density target exempts header-shaped and small public-API packages; voice rules apply to all Claude-authored docs, not only Go source | 0141, 0142, 0148, 0168, 0169, 0170, 0173 |
| JMAP per-folder baseline pull on nil SyncToken — Email/query paged by inMailbox + sentinel-id Email/get for state in the same roundtrip; FetchHeaders chunked at 500 | 0143 |
| Catkin — core, live styling, commands, QoL, annotation pipeline + spellcheck, render-cursor splice; cleanup + lint sweeps + popover overlay padding | 0144–0147, 0149–0152, 0154, 0155 |
| Catkin all-value path — Model and Buffer value types; With* setters; textarea sealed inside Buffer; Elm boundary enforced by types | 0212 |
| Compose holds catkin directly — `mailcompose.Editor` interface + `CatkinEditor` adapter deleted; `compose.Model` embeds `catkin.Model` value | 0213 |
| `App.Update` decomposition — `app.go` shrinks to model + 6-way dispatcher; per-domain `app_{chrome,outbox,compose,modals,contacts}.go` each owning `updateXMsg(msg) (App, tea.Cmd, claimed bool)`; chrome runs first (drainer/idle hot path); `armToast` and `openNewCompose` unify duplicated call sites surfaced by the split | 0214 |
| Compose foundation + Send/Append — Editor interface + CatkinEditor adapter, Draft, AssembleMIME (multipart/alternative via filter.MarkdownToHTML, multipart/mixed for attachments), Seed{Reply,ReplyAll,Forward} parsing parent headers from raw bytes; `Send`/`Append` on mail.Backend (JMAP `Email/import` + `EmailSubmission/set`; IMAP lazy `emersion/go-smtp` + APPEND); `[account.smtp]` provider preset defaults; `mailimap.ProbeSMTP` | 0156, 0157 |
| Cache outbox Send/Append dispatch — schema v6 adds `outbox.payload`; `SendArgs{Envelope}` + `AppendArgs{Flag}`; `QueueSend`/`QueueAppend` payload-bearing entry points; drainer `dispatch(args, row)` routes to `Backend.Send`/`Append`; `revertOptimisticTx` no-ops on Send/Append so `DiscardOp` works; IMAP enqueues two ops, JMAP one | 0158 |
| Path-scoped subsystem invariants — Cache, Catkin, Attachments split into `.claude/rules/<name>-invariants.md`; extraction-readiness criteria (settled, ≥ ~25 lines, natural path scope) | 0153 |
| ComposeTab — App-owned inline compose surface, Tab/Shift+Tab focus cycling, Esc as focus toggle, Ctrl+X send + Ctrl+C cancel; cache.Account.QueueOutbound (one op JMAP, two ops IMAP) + Backend.IsJMAP() predicate; TidyFn function-pointer seam on App | 0159, 0160 |
| internal/ui/ package layout — bubbles-shaped subpackages (account, compose, helppopover, messagelist, movepicker, reader, sidebar) plus uicore sibling for shared chrome; mail.FolderEntry hoisted; per-package Msg-namespace policy; *Tab suffix dropped; ErrorMsg + TriageOp + ComputeLayout + NewSpinner hoisted to uicore; account-scoped cmds (folder/cache/triage/sweep) lifted; AccountTab → account.Model | 0161, 0162, 0163 |
| Address book — `internal/ui/contacts/` package; fixture-backed; popover, T9 sidebar + List + RenderDetailCard for Contacts mode (9.1a); `Form` (Person/Business, two render contexts, focusList model, `Ctrl+S` validate, `Esc`/dirty-confirm via `pendingFormDiscard`) (9.1b); compose autocomplete `Dropdown` + `SuggestFn` seam threaded through `compose.New`/`Open`, fixture-backed via `contacts.FixtureSuggestions`, splices below the focused header (9l) | 0166, 0167, 0174 |
| CardDAV ingest — `internal/contacts/` (Client + vCard parser + Sync + Store seam); schema v8 (addressbooks/contacts/contact_emails/contact_phones/message_recipients) with backfill from existing messages; sync-collection / CTAG / full-pull strategy; recency-decayed `SuggestAddresses` LIMIT 7; `[[account.contacts]]` config block with credential fallback to parent; `phonenumbers.Parse` validation in Form; compose autocomplete + i-popover read from cache; fixture pool retired from those paths | 0175 |
| CardDAV write-back — `KindContactPut`/`KindContactDelete` outbox kinds + `ContactPut`/`DeleteArgs`; `contacts.Writer` seam wired once at App init onto `Account.ContactsWriter`; `PatchVCard` mutates only poplar-owned keys (FN/N/ORG/TITLE/NOTE/EMAIL/TEL/REV/KIND/UID), unknown fields round-trip verbatim; `BuildVCard` for new contacts; schema v9 nullable `outbox.folder` (drops sentinel-folder workaround); sentinel matrix `errors.Is(contacts.ErrAuth/ErrNotFound/ErrPreconditionFailed)`; 412 → `OpConflict precondition-failed`, NotFound on Delete idempotent; `D` key in Form gated on existing UID + non-text-input focus; confirm cascade form-discard > contact-delete > compose-save > empty-folder; `LookupContact` returns UID; `LoadStoredVCard`/`DefaultBookHref` cache helpers | 0176 |
| Drafts persistence — schema v7 `drafts` (server_uid/server_folder paired), gob payload via `compose.EncodeDraft`/`DecodeDraft`; `KindPushDraft` outbox op + `QueuePushDraft`; `mail.Backend.PushDraft` on both backends (JMAP one-request `Email/import`+`Email/set destroy`; IMAP `APPEND \Draft` + best-effort prior-image expunge); cache writes split — `CreateDraft` insert-or-update vs. `UpdateDraft` UPDATE-only (0-row = race-immune discard); `ErrDraftSuperseded` → drainer publishes `CacheEvent.Note` "draft superseded by another client", App routes non-empty Notes to error banner; `compose.Model` 1s autosave + 5min push tick; App routes Drafts-folder Enter through `LookupDraftByServerUID` or `draft:<id>` synthetic UIDs; unified Save/Discard/Keep modal | 0164, 0165 |
| TCP keepalive on stdlib `net.KeepAliveConfig` (vendored aerc keepalive helper removed) | 0171 |
| Decision index extracted from invariants.md into `docs/poplar/decisions/INDEX.md` | 0172 |
| Per-account identities + signatures — `[[account.identity]]` blocks with ordered `[[account.identity.signature]]` sub-blocks (`text`/`file` mutually exclusive, RFC 3676 sentinel injected at config-load); legacy top-level `from` synthesizes one identity; first-in-order is default; `compose.Draft.Identity`/`Signature` (-1 = no sig) thread through `AssembleMIME`; JMAP `Backend.identityIDs` keyed by lowercased email, one probe populates the cache; compose `focusFrom` chip + cycler (`Space/←→/h/l` identity, `Ctrl+G` global / bare `g` in focusFrom signature) | 0177 |
| Claude Tidy — user-invoked `Ctrl+T` body proofread (supersedes ADR-0159's `TidyFn`/send-path tidy clause); pure `tidy.DiffRanges` rune-LCS character diff with replace-block expansion; `catkin.tidyAnnotator` + `KindTidyChange` + `Styles.TidyChange` (`AccentPrimary` underline) painted via `Model.SetTidyHighlights`, self-clears on first body mutation; `compose.Editor.SetStyles`/`SetTidyHighlights` seam; `[ui.tidy]` config block (`enabled`, `[ui.tidy.api]`/`[ui.tidy.rules]`/`[ui.tidy.style]`) merged onto `tidy.DefaultConfig()` with `tidy.Validate`; App threads `(enabled, apiKey, cfg)` into compose via `SetTidy` at every `New`/`Open`; gated footer hint `^T tidy` rank 6 visible when `tidyEnabled && focusBody`; `composeSendCmd` no longer carries a tidy parameter | 0178 |
| Compose attach picker — multi-select TUI file browser overlay (`Ctrl+O`), `bubbles/filepicker`-shaped vocabulary reimplemented for multi-select; async `readDir` + id-guard; view-state stack on ascend; `Space` toggles, `a` accepts, `Enter`-on-file shortcut, `.` toggles hidden; chip row below Subject with `focusAttach` slot skipped when empty; `d`/`⌫`/`⌦` remove; `AttachAcceptedMsg`/`AttachCancelledMsg` cross-boundary; picker rides inside compose's input window (outside the global modal cascade) | 0179 |
| Upstream displaywidth override hook — investigation closed; vendored `internal/ansix/` shim chosen over an upstream PR; maintainer signaled wrapper preference (PR #23 comment 2026-05-02), zero external-contributor merges in seven months; fork prototype parked at `~/Projects/displaywidth/add-overrides`; ansix lands in Pass 9w.1 | 0180 |
| `internal/ansix/` width adapter — SPUA-aware shim over `charmbracelet/x/ansi`; exports `Width`/`Truncate`/`TruncateEllipsis`/`PadOrTruncate`/`SpuaCount`/`SPUACellWidth`/`SetSPUACellWidth`; `uicore.FillRowToWidth`/`ApplyBg` route through it; ADR-0084 JoinHorizontal ban stands (lipgloss-internal `Width` calls unreachable from a non-fork wrapper); 16 callsites migrated off `uicore.Display*` | 0181 |
| Bubble-eval roadmap closed — Eval A + Eval B + 9x.1/9x.2/9x.3 harvests over nine libraries all landed Keep + harvest; one inline adoption (movepicker filter-match rune highlight) and one carry-forward candidate (`bubbles/help` `shouldAddItem` truncation); future evals require a named flip condition from `docs/poplar/research/2026-05-08-bubble-consolidation-verdict.md`, not a fresh rubric pass; new (unsurveyed) libraries still get a fresh rubric run | 0182 |
| Undo-send — schema v10 adds `outbox.scheduled_for` (user-intent dispatch hold) and `outbox.draft_id` FK; drainer pickup gate AND-clauses `scheduled_for`; `outbox_pickup` rebuilt with `scheduled_for` as the leading key; drainer deletes linked drafts row inside the OpDone tx on Send/Append success; `cache.CancelOps` atomically deletes pending rows or returns `ErrNotPending`; `[ui] undo-send-window` default 10s, range `[0, 5m]`; `composeSendCmd` persists a draft + threads `scheduled_for` through `QueueOutbound`; `SentMsg{OpIDs, ScheduledFor, Draft}` arms `pendingAction` with a 1Hz countdown banner; `u` routes by op kind to `undoSendCmd` → `CancelOps` + `RestoreFromDraftMsg` reopens compose from the in-memory Draft | 0183 |
| Schedule send — compose `Ctrl+L` opens `SchedulePicker` (3 presets + Custom textinput); `compose.ParseSchedule` is a pure function over (`s`, `now`) accepting ISO/US/English/keyword/offset shapes; `cache.RescheduleOp` updates `outbox.scheduled_for` iff still pending and not yet eligible; `cache.OutboxScheduled` joins outbox→drafts via `draft_id`, decodes Subject from first 4 KB of payload via `net/textproto`; sidebar `SetOutboxCount` injects synthetic `mail.CanonicalOutbox` entry above Disposal when count > 0 (uses `Folder.Unseen` for the badge); `internal/ui/outbox` subpackage owns the read-only list view with `c`/`s`/`e`/`Esc` bindings; App owns a separate `pendingReschedule{picker, opID}` for the reschedule path so `ScheduleAcceptedMsg` routes to `RescheduleOp` instead of `dispatchSend`; explicit user-scheduled time bypasses ADR-0183's undo-send window | 0184 |
| `charm.land/v2` substrate — mechanical migration to `charm.land/{bubbletea,lipgloss,bubbles}/v2` (imports, `KeyMsg`→`KeyPressMsg`, `Sequentially`→`Sequence`, `spinner.NewModel`→`New`, `viewport.New(w,h)` positional → option form, exported `Width`/`Height` fields → methods); KeyPressMsg field-layout drift (`Type`/`Runes`/`Alt`/`Paste` → `Code`/`Text`/`Mod`); `lipgloss.AdaptiveColor` removed (palette + theme fields concrete `color.Color`); transitional `App.View()` returns `tea.NewView(s)` as the published seam to 13.2b; CLAUDE.md no-scars rule for migrations | 0189a |
| `charm.land/v2` reframes — `tea.PasteMsg` arms in catkin (single-undo splice via `undoRing.push`; URL-wrap when payload is whitespace-free `http://`/`https://`/`mailto:` token at a cursor inside a word) and compose (address-field atomic chip emission via `content.ParseAddressList` with `commitAddrField` / `markEdited` extracted from `acceptSuggestion`; subject + body delegation); records the realized chrome + cursor seam (declarative `v.AltScreen`/`v.WindowTitle`, `Cursor() *tea.Cursor` accessors, `App.frameCursor()` focus walk, `SetVirtualCursor(false)` app-wide) absorbed by 13.2a ahead of plan | 0189b |
| Probe transcripts + typed config errors + canonical config writer — shared `mail.ProbeResult{Steps, Err}` (`internal/mail/probe.go`), `mailimap.Probe` (5 steps incl. STATUS INBOX via SELECT) routing through `probeDial` test seam, `mailjmap.Probe` (3 steps; library bundles TLS + bearer + Session/get) routing through `probeAuth`; `mailimap.layerTLS` extracted from `dial()` for shared use; `config.Provider.CredentialStrategy` + `HelpURL` per preset; typed `*config.ConfigError{Path, Line, Account, Field, Message, Suggest}` (sentinel `ErrConfigInvalid`) for the four user-facing validators (unknown provider, missing host, missing source, missing smtp.host); `AccountConfig.Name` defaults to `Email` when omitted (#29); `config.Render(accts, ui, cache) []byte` canonical TOML emitter with `[account.smtp]` ordered before `[[account.identity]]` to dodge TOML's array-of-tables rebind | 0190 |
| First-run wizard — `internal/wizard/` UI-free domain (`Model`, `SelectStrategy(preset) → config.CredentialStrategy`, `Apply(Model) → AccountConfig`, `Probe(ctx, cfg)` dispatcher with `imap`/`jmap`/`smtpProbeFn` test seams); `internal/ui/wizard/` bubbletea + huh surface with section registry (`account` → `theme` → `confirm`); huh.Form for static pages, custom `tea.Model` probe screen; `HuhTheme` adapter projects `theme.CompiledTheme` into `huh.Styles`; `art/poplar-logo.ans` cbonsai artifact embedded for future swap behind a typographic wordmark; `AccountConfig.Preset` field round-trips preset names through `config.Render` (writer prefers `Preset` over `Backend`); `mail.IsSelfHosted(host)` shared with mailimap; atomic-write of new config; `poplar config init --interactive [--section=…]` cobra wiring | 0191 |
| First-run integration in runRoot — `ErrFirstRun` auto-launches the wizard (template removed first; `--no-wizard` / `POPLAR_NO_WIZARD=1` opt out to exit-78); `ConfigError` formatted with `--repair=<acct>` hint; `--repair=<name>` via `wizard.FromAccount` reverse-applies preset/label/identity/self-hosted endpoint, runs the account section only, bypasses the wizard's single-account writeConfig, and splices `RepairResult` through `config.Render` + atomic rename so other `[[account]]` blocks survive | 0192 |
| `bubbles/v2/list` adoption for the picker family — `reader.LinkPicker`, `reader.AttachPicker`, `movepicker.Model` each hold a `list.Model`; custom `list.ItemDelegate` per consumer renders bespoke item shapes; `uicore.NewListStyles` projects the compiled theme onto `list.Styles` shared by the three; `uicore.PickerListSize` + `SplitAndPad` factor out the box geometry + Box post-processing; movepicker's `*modelCache` (ADR-0130 escape hatch) removed (list.Model owns viewport state); always-on-filter friction requires sync `SetFilterText`/`SetFilterState` apply on Open and on filter-change in Update, plus j/k/up/down intercept under `Filtering` (upstream `updateKeybindings` disables list nav while filtering) | 0194 |
| `compose/attachpicker` — deliberate deviation from `bubbles/v2/filepicker`: multi-select model + icon UX + ModalShell wrap don't compose with filepicker's single-select + permissions columns + freestanding View. Three patterns lifted from upstream without importing it: symlink resolution via `e.Type()&os.ModeSymlink` + `filepath.EvalSymlinks`/`os.Stat`, atomic package-level id counter (`nextAttachID atomic.Int64`), right-aligned 7-cell size column via `lipgloss.Right` | 0195 |
| Modern Go stdlib defaults — `go-conventions` skill names preferred 1.21–1.26 forms (`slices.SortFunc`/`slices.Sort`/`sync.OnceValue`/`iter.Seq`/`log/slog`/`for range N`/`maps.Keys`/`cmp.Or`/`errors.Join`/builtins/loop-shadow delete); `/simplify` Agent 3 surfaces semantic candidates; `scripts/modern-go-check.sh` is the grep-tier counterpart to `voice-check.sh` (M1 `sort.Slice(Stable)`, M2 `sort.Strings`/`Ints`/`Float64s`, M3 three-clause `for i := 0; i < N`, M4 package-scope `sync.Once`); wired into `make check`, soft-warn through the 16-series (`MODERN_GO_STRICT=1` flips locally), 16d closes by removing the soft-warn gate; 16b/16c/16d apply the new defaults to ~60 audited sites; ADR-0046's `internal/theme/` hex-literal exemption stands | 0196 |
| `log/slog` adoption — `internal/` diagnostic logging via `slog.NewTextHandler`; `installLogger` in `cmd/poplar/main.go` before cobra runs; TTY → `$XDG_STATE_HOME/poplar/poplar.log`, non-TTY → stderr, open-failure silent fallback; `POPLAR_LOG=debug` for `LevelDebug`; trailing `*slog.Logger` arg on `mailjmap.New` / `mailimap.New` / `cache.Open` (nil → `slog.Default().With("component","<pkg>")`); CLI/UX strings in `cmd/poplar/` stay on `os.Stderr` | 0197, 0209 |
| Small-refactor sweep — `ansix.Measurer` value type replaces the package global (`SetSPUACellWidth` / `SPUACellWidth` deleted), threaded from `cmd/poplar/root.go` through `ui.NewApp` into every subpackage Model; `uicore.FillRowToWidth` takes a Measurer; `WithLogger` functional-options collapsed to plain `*slog.Logger` args; `internal/backoff/` and `internal/humanize/` folded into per-package helpers (`expBackoff`, `humanBytes`) with the defensive `<= 0` clamps dropped | 0209 |
| Pre-beta outbound cleanup — drop `mail.MessageInfo.Date string` (cache schema v12 `ALTER TABLE messages DROP COLUMN date_str`; SentAt is sole date carrier; reader/messagelist/compose fallbacks deleted); rename `internal/compose` → `internal/mailcompose` (matches `mailimap`/`mailjmap`/`mailauth` family; eliminates the `mailcompose "...compose"` alias at every importer); rename `internal/tidy` → `internal/tidytext` and drop CLI surfaces (`LoadConfig`, `ApplyRuleOverrides`, `ApplyStyleOverrides`, `ConfigString`); TOML `[ui.tidy]` → `[ui.tidytext]`; `UIConfig.Tidy`/`TidyConfig` → `Tidytext`/`TidytextConfig`; `tidytext.Validate` collects via `errors.Join`; `cache.messageInfoCols` factored as the shared scan target | 0203 |
| `internal/strdist/` — one `Levenshtein(a, b string, limit int) int` consolidating `config.levenshtein` (uncapped) and `catkin.editDistance` (capped); `limit ≤ 0` disables the cap (treats as `math.MaxInt`); two callsites route through it; `suggestProvider`'s redundant `bestDist > 2` post-loop guard deleted | 0204 |
| Audit A outcome — Phase A bug-fix-completeness sweep returns 11 non-blocking findings (0 blocking); Batch 2 proceeds without a 26.1 remediation pass; findings collapse into BACKLOG #54 (mailjmap network-error sentinel gap), #55 (mailjmap `b.mu` held across HTTP round-trip), #56 (mailimap Gmail-Destroy stale `b.current`), #57 (config-validator gaps incl. non-strict TOML), #58 (defensive-clamp cleanup sweep) | 0210 |
| Audit A remediation — `mail.IsConnectionDead` hoisted into `internal/mail/`; both backends route `classifyErr` through it; mailjmap `refreshFolders` snapshot-release-reacquire pattern around `b.mu` (matches `resolvedPassword`); mailimap Gmail Destroy captures `prev := b.current` and re-Selects on return via defer; `internal/config/strict.go` strict-TOML decoder + Levenshtein sibling-suggestion via reflection (rejects unknown keys with `*ConfigError` carrying a `Suggest`); `validateAuth` (`plain`/`login`/`cram-md5`/`xoauth2`/`bearer`) + `validateOAuthStore` (`keyring`/`age-file`) enum guards; bare `provider = "imap"` requires `port != 0`; contacts credentials validated post-fallback; `contacts.url == ""` surfaces as "required"; eight defensive nil/clamp checks deleted from `internal/cache/` and `internal/ui/status_bar.go` (`ContactsWriter == nil` kept with rationale comment) | 0211 |
| Audit B.1 outcome — Elm + bubbletea v2 conformance sweep returns 4 findings (0 blocking): wizard `writeConfig` I/O lifted into a `tea.Cmd` arming on `writeConfigDoneMsg`; `catkin.NewBuffer` adds the missing `SetVirtualCursor(false)` (literalizes the ADR-0189b claim — catkin owns its render path so the textarea's virtual cursor was inert but the claim now matches code); `messagelist.Model.Update` and `reader.Model.Update` add `WindowSizeMsg` arms forwarding into the embedded `bubbles/v2/list.Model` / `viewport.Model` (forward-compat — no v2 bubbles consumer today); receiver-discipline gap across `internal/ui/` subpackages filed as ROADMAP project `ui-all-value` (8-subpackage multi-pass remediation, ordered messagelist → sidebar → compose → account → reader → contacts → outbox → wizard) | 0215 |
| Audit B.2 outcome — general structural integrity returns 1 apply-inline + 1 note-only (0 blocking, 0 ROADMAP): `App.updateKey` split into `routeOverlayKey` (overlay cascade, 70 lines, claimed-flag return) + `updateGlobalKey` (shortcut switch, 104 lines), `updateKey` now a 10-line chain — every `internal/ui/` `Update`-side function under the 150-line threshold; six `internal/ui/` non-test files over 600 lines (messagelist 1111, compose 1066, contacts/form 991, account 873, cmds 685, sidebar 643) each named with a Model-state reason and left for the `ui-all-value` project to re-touch; dispatch exhaustive (no back-channel coupling, no subpackage→parent imports); two interfaces in `internal/ui/` after Editor deletion (`compose.CacheStore` documented test seam, `wizard.section` real polymorphism) | 0216 |
| v2 declarative `tea.View` fields — `App.View()` sets `ProgressBar` (fixed priority ladder attachment > outbox > sync, sourced from `cache.Account.{AttachmentDownloadProgress,OutboxDrainProgress,SyncProgress}`), `ReportFocus = true` (toggles `App.focused` via `FocusMsg`/`BlurMsg`), `KeyboardEnhancements.ReportEventTypes = true` (`KeyboardEnhancementsMsg` lands in `App.kbdCaps`; Flags bitmask checked via `SupportsKeyDisambiguation`/`SupportsEventTypes`); `uicore.GatedBinding{Binding, RequiresKittyKbd}` tags chord bindings whose semantics need Kitty disambiguation; `catkin.ChordSet()` returns the full vocabulary, `catkin.ActiveChords(disambig)` projects the renderable subset for helppopover Compose context and the compose footer hint; unfocused-only new-mail toast (`[ui] new-mail-toast`, default true) coalesces arrivals across a 1s `coalesceTimerMsg` window into `pendingAction.newMailCount` rendered as "· N new from Foo ·" (single sender) or "· N new in Inbox ·" (mixed); FocusMsg clears the new-mail toast variant only | 0217 |
| Mouse support — reader pane only (Pass 33); `tea.View.MouseMode = MouseModeCellMotion` declared every frame; `App.updateMouse` cascade absorbs while any overlay is open, translates to right-pane-local coords and forwards via `account.Model.UpdateViewer` when viewer is ready, inert otherwise; `reader.Model` carries `chipHits` + `bodyHits` populated at `layout()`; wheel-in-body forwards to the embedded viewport; click on a chip emits `OpenAttachmentMsg`; click on a `[N]: <url>` ribbon row emits `LaunchURLMsg` (the inline `[^N]` glyph is not a click target); `content.RenderBodyWithFootnotes` returns a third `[]FootnoteRow{Row, PickerIndex}` so reader never re-walks the body; sidebar + cross-pane mouse deferred to Pass 34 | 0218 |
| Mouse support — sidebar + cross-pane (Pass 34); mouse is one-to-one keyboard shorthand; `App.updateMouse` drops the viewer-ready guard, translates Y to account-local and branches on `viewerOpen` × pane region — viewer-ready right pane → `UpdateViewer`, viewer-open sidebar click → `CloseViewer()` then `account.Model.Update`, else → `account.Model.Update`; account partitions by X into `handleSidebarMouse` / `handleRightPaneMouse`; sidebar click on a real folder = `J`/`K`, on a synthesized intermediate = toggle expand; right-pane click = `Enter` (cursor + `openMessage`); wheel = the cursor key per notch; `messagelist.Model.ItemIndexAt` and `sidebar.Model.RowAtLineOffset` are the row-level hit-test seams; tree expand/collapse on real parents stays keyboard-only | 0219 |
| Audit C outcome — feature surface (Pass 36) returns 2 P0 inline + 2 P1 queued for Pass 36.1 remediation (0 blocking): SMTP `smtpAuth` lacks `ForceRefresh`-on-`ErrAuth` retry that IMAP `dial` has (Finding A, queued); `App.updateMouse` routes right-pane clicks to the viewer when `m.compose != nil && m.viewerOpen` because routing keys on `viewerOpen` while `renderFrame` keys on `m.compose` (Finding B, queued); `BeginSync`/`EndSync` in `internal/ui/account/cmds.go` wrapped in immediately-invoked funcs with `defer` so panics cannot orphan `syncInFlight` and the OSC-9;4 indeterminate bar (Finding C, inline); `docs/poplar/audit-plan.md` §"Phase C" drops the `v.ReportFocus` pause/resume focus item — describes a JMAP push / IMAP IDLE pause-on-blur contract that the codebase does not implement (Finding D, inline) | 0221 |
| Audit C remediation (Pass 36.1) — `mailimap.classifyErr` extends to `*gosmtp.SMTPError` 530/535/538 → `mail.ErrAuth`; IMAP `dial` wraps `authenticate` through `classifyErr` so the existing `errors.Is(_, mail.ErrAuth)` retry actually fires; SMTP `smtpAuth` mirrors the IMAP ForceRefresh-and-retry pattern via a new `smtpAuther` test seam; both compose-mount sites (`openNewCompose` + `m.keys.Compose` branch) call `m.acct.CloseViewer()` + `deriveChromeFromAcct` when `m.viewerOpen`, so `updateMouse`'s right-pane predicate matches `renderFrame`'s rendered surface | 0222 |
| Audit D outcome — database (Pass 37) returns 3 P1 queued for Pass 37.1 + 1 P2 noted only (0 P0, 0 blocking): `message_recipients` lacks `ON DELETE CASCADE` so deleted-message recipients bias `SuggestAddresses` autocomplete (F1, P1); `messages_fts` virtual-table rows orphan on `DELETE FROM messages` — `Search`'s inner JOIN filters them but storage bloats (F2, P2); `migrateV11` backfills FTS headers only, leaving body column empty for messages whose body was cached pre-v11 — silent body-search miss until eviction (F3, P1); `mailimap.Changes` documents UIDVALIDITY re-anchor and reserves SyncToken bytes 0–3 but never reads UIDVALIDITY from `SELECT`/`EXAMINE`, so a server-side UIDVALIDITY change leaves the cache anchored to stale UIDs (F4, P1) | 0223 |
| Audit D remediation (Pass 37.1) — schema v13 rebuilds `message_recipients` with `REFERENCES messages(id) ON DELETE CASCADE` (JOIN-copy drops pre-existing orphans) and scrubs orphan `messages_fts` rows (F2 absorbed for free); `migrateV11` extended with `backfillFTSBodies` looping `bodies.bytes` through `content.ExtractPlainText` + `writeFTSBodyTx`, parse/write skips logged via slog debug rather than aborting; `mail.Folder.UIDValidity uint32` carries the RFC 3501 value (zero for JMAP); `mailimap.realClient.Select` populates it, `OpenFolder` captures into `b.currentUIDVal`, `Changes` decodes SyncToken bytes 0–3 and returns `mail.ErrCannotCalculateChanges` on mismatch (`encode`/`decodeIMAPToken` widen to the `(uidvalidity, maxuid)` pair) | 0224 |
| Audit E outcome — specified vs. assumed (Pass 38) walks 212 active ADRs and returns 121 chosen / 79 defaulted-still-right / 12 defaulted-and-wrong; 3 P1 + 8 P2 + 1 already-addressed findings (0 P0, 0 blocking): movepicker `↑`/`↓`-only navigation breaks vim-first consistency where sidebar-search's `Tab` mode-toggle works (F2, P1, ADR-0091); `outbox.payload` has no per-row or per-account size cap (F4, P1, ADR-0158); OAuth foreclosed device-code fallback so SSH-tunnel / container / NAT users can't complete loopback PKCE (F6, P1, ADR-0193); ADR-0003 reads `status: accepted` despite being contradicted by ADR-0034 (F1, P2 hygiene bundled into 38.1); legacy XOAUTH2 `password-cmd` resolver runs on every dial (F3, P2, ADR-0108); hand-rolled date parser will accrue vocabulary gaps (F5, P2, ADR-0184); FTS5 stored-content duplicates body plaintext on disk (F7, P2, ADR-0188); markdown body width 72 hardcoded (F8, P2, ADR-0066); long-bare-URL footnote threshold 30 cells (F9, P2, ADR-0085); zero-length attachment results uncached (F10, P2, ADR-0135); catkin word-level undo coalescing (F11, P2, ADR-0147); draft last-write-wins (F12, already addressed by ADR-0165) | 0225 |
| Outbox per-row size cap — `[cache] max-outbox-bytes` config field (default 0 = unlimited, mirrors `max-size`); `cache.insertFolderOp` rejects oversize payloads with `cache.ErrOutboxRowTooLarge` before INSERT; `QueueOutbound` propagates; writer round-trips when non-default; inline fix threads `MaxAttachmentSize` + `MaxOutboxBytes` through `cmd/poplar/root.go` alongside `MaxSize` | 0226 |
| OAuth device-code fallback — RFC 8628 device-code as opt-in alongside ADR-0193's loopback PKCE default; `OAuthDefaults.DeviceAuthURL` propagates Google + Microsoft device endpoints; `mailauth.Client` splits the flow into `RequestDeviceAuth` + `PollDeviceCode` (with `slow_down` +5s per RFC 8628 §3.5) plus an `AuthorizeDeviceCode` single-shot wrapper; wizard adds inline `huh.NewSelect` consent-method radio when preset supplies the endpoint; loopback failure screen adds `[d] switch to device code` affordance; new `oauthStageDevice` stage paints user_code + verification URI while polling; `[account] oauth-mode = "loopback"`/`"device-code"` round-trips via `validateOAuthMode`; supersedes ADR-0193's "no device-code in v1" clause (rest of 0193 stands) | 0227 |
| Audit F outcome — sharp edges + insecure defaults (Pass 39) returns 0 P0, 8 P1, 14 P2 across 21 packages walked in four parallel batches (mail / storage / UI / config); dominant pattern is `_ =` error suppression on write-back and terminal-state paths, not bad config defaults: OAuth consent server lacks HTTP timeouts (F-F-1); `smtpDial` discards caller context (F-F-2); device-code zero `ExpiresIn` → immediate `ErrConsentTimeout` (F-F-3); cache `storeBody`/`storeAttachments` errors dropped on read-path write-backs (F-batch2-1); drainer `finalizeSuccess`/`finishOp` errors dropped leaving ops stuck in `OpExecuting` (F-batch2-2); `SyncFolder` error swallowed in `queryFolderCmd` (F-batch3-1); `buildOAuthClient` ignores `acct.OAuthStore` (F-batch4-1); `writeAtomically` in `config_discover_folders.go` skips `chmod 0600` (F-batch4-2); 14 P2s logged in plan doc + BACKLOG | 0228 |
| Audit F remediation (Pass 39.1) — loopback OAuth `http.Server` gets `ReadHeaderTimeout: 10s` / `WriteTimeout: 5s`; `mailimap.ProbeSMTP` + `smtpDial` + `Backend.smtpClientLocked` thread `ctx`; `RequestDeviceAuth` floors `ExpiresIn` to 300 when the server returns ≤ 0; `cache.FetchBody`/`FetchAttachment` log write-back failures via `a.log.Warn`, `cache.Attachments` propagates `storeAttachments` errors; `cache/drainer.executeOne` routes every `finalizeSuccess`/`finishOp` through `Account.logTerminal` (Error-level on non-nil); `ui/account.queryFolderCmd` surfaces `SyncFolder` errors as `uicore.ErrorMsg`; `mailauth.OpenStore` grows a `preferred Backend` param, `cmd/poplar` and `internal/ui/wizard` `buildOAuthClient` pass `acct.OAuthStore` / `state.OAuthStore` so the configured store wins over the keyring probe; `cmd/poplar/config_discover_folders.go:writeAtomically` chmods the tempfile to 0o600 before rename, mirroring `writeConfigAtomic` | 0229 |
| Audit G outcome — test assertion meaningfulness (Pass 40) returns 0 P0, 21 P1, 17 P2 across 22 packages and ~185 test files walked in four parallel batches; dominant pattern is silent-success fakes (`MockBackend.Send`/`Append`, `mailimap.fakeClient.Copy`/`Move`/`UIDExpunge`, `mailjmap.fakeClient.Do`, `blockingBackend`, `fakeCache.CreateDraft`/`UpdateDraft`, `fakeContactsWriter.Multiget` all return nil unconditionally); secondary pattern is trivially-true lipgloss `Render("x") != ""` assertions; tertiary is `t.Skip` placeholders (all four `contacts.Sync` paths). 21 P1s queue Pass 40.1; P2s land in BACKLOG | 0230 |
| UPSERTs that read the rowid downstream use `RETURNING id` — `LastInsertId()` is connection-scoped to the most recent real INSERT and returns stale values on UPDATE branches of UPSERTs (sometimes deleted rowids → FK failure, sometimes wrong-but-living rowids → silent junction-table corruption). `cache.upsertMessages` switched to `tx.QueryRow(... RETURNING id).Scan`; plain-INSERT paths in `cache/ops.go` keep `LastInsertId()` since they always insert | 0231 |
| Mutation testing as `make check-deep` — `scripts/check-deep.sh` runs `gremlins unleash` per package across a curated list of logic-heavy packages (`mailcompose`, `mail`, `cache`, `content`, `filter`, `tidytext`, `mailauth`, `config`); UI / network-backend / terminal-probing packages excluded; not in `make check`, runs at pass-end; initial thresholds 0% (informational) pending first-run calibration; `go-conventions` skill grows an "Assertion Discipline" subsection codifying Audit G patterns (silent-success fakes get per-method `*Err error` fields; no SUT-derived expectations; no `Render("x") != ""` assertions; Cmd assertions invoke + type-switch; no unconditional `t.Skip`); AST-based unconditional-`t.Skip` detector queued as BACKLOG #59 | 0232 |
| Audit G remediation (Pass 40.1) — 21 P1 test-assertion fixes ship together: per-method `*Err error` injection on six fakes (`mailimap.fakeClient` copy/move/expunge/append, `MockBackend.SendErr`/`AppendErr` + named `SendCall`/`AppendCall` records + `Emit`/`CloseUpdates` test helpers, `mailjmap.fakeClient.Do` returns hard error on nil `respond`, `blockingBackend` move/destroy/flag/send/append, `compose.fakeCache` create/update/load, `fakeContactsWriter.Multiget`); 11 assertion tightenings (`tok1 == tok2`, poll-fallback `UpdateFolderInfo` count via `Backend.pollInterval` field replacing package var, JMAP Import request-shape capture, CardDAV `PutAddressObject_Auth`, re-armed pump `backendUpdateMsg` type-switch via `MockBackend.Emit`, popover col 56 hard-coded, attachpicker batch walked via shared `collectMsgs`, first-run template-write vs `template.golden`, `runReauth` returns `errUnknownReauthAccount`/`errReauthAccountNotOAuth` sentinels mapped to exit-78 by `main`, JMAP-routing asserts SMTP fake uncalled, evict test seeds valid config); lipgloss style attribute assertions (`GetForeground`/`GetBackground`/`GetBold`/`GetItalic`) replacing `Render("x") != ""`; four real `contacts.Sync` tests against `httptest.NewServer` wrapping `carddav.Handler` + `syncTestBackend`; `voice-check.sh` grows `scan_excl_tests` carve-out so test fakes can satisfy third-party Get-prefixed interfaces | 0233 |
| check-deep calibration + AST skipcheck — per-package mutation efficacy floors in `scripts/check-deep.sh` set to observed minus 5pp buffer (`mailauth` 71, `content` 73, `filter` 77, `mail` 64, `cache` 72, `tidytext` 74, `mailcompose` 78, `config` 78; baseline 2026-05-16); `scripts/skipcheck/` is an AST walker rejecting `func Test*(t *testing.T)` whose body's first statement is an unguarded `t.Skip`/`SkipNow`/`Skipf` on the actual `*testing.T` parameter name (not just `t`); wired into `make check` between `modern-go-check` and `test`; closes BACKLOG #59 | 0234 |
| `internal/mail` mutation efficacy lifted to 94.44% (Pass 40.4) — new `TestIsConnectionDead`, `TestConfigKey`, `TestClassifyDisposition`, `TestIsSelfHosted`; extended `TestMockBackend_QueryFolder` with `at end` boundary + cap assertion; `TestMockBackend_FetchBody_SeededUIDs` covers the `mockBodies` literals; floor raised to 89; two equivalent mutants documented and accepted (`mock.go:117` clamp boundary, `probe.go:28` `iota+1`) | 0235 |
| `make check-deep` flag calibration — gremlins runs with `--timeout-coefficient 10 --workers 1` in `scripts/check-deep.sh`; prior baselines (Pass 40.3) measured under the wrong flags inflated efficacy by excluding timed-out mutants from the denominator; `internal/mailauth` floor lands at 73 (78.50 observed − 5pp) after seven coverage-lift tests (`TestDefaultDevicePollInterval`, `TestXOAuth2_NextDecodesError`/`RejectsBadJSON`, `TestConsentServer_PassesError`/`PassesCode`, `TestListen_ScansPortRange`/`ExhaustedRange`); other-package floors stay put until Pass 40.5b re-measures them | 0236 |
| `internal/mailauth` mutation lift to 99.07% + seven-package floor recalibration (Pass 40.5b) — 22 of 23 survivors killed via targeted tests in `devicecode_test.go`/`loopback_test.go`/`oauth_test.go`/`tokenstore_test.go` (form-shape boundaries, `ExpiresIn`/`Interval` zero-defaults, expiry arithmetic, server-timeout constants, `RefreshToken` rotation via table-driven `countingStore` cases, 500+`invalid_grant` status guard, `keyring.MockInit`/`MockInitWithError` for `KeyringStore` and `OpenStore` probe paths); one equivalent mutant documented (`oauth.go:213` `time.Until > 5*time.Minute` boundary, untestable without a clock seam); recalibrated floors `mailauth` 94, `content` 72, `cache` 95 (jumped from 72 under the bad flags), others unchanged | 0237 |
