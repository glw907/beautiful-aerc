# Pass 8.5 B — Overengineering Audit: `internal/cache/`

**Date:** 2026-05-03
**Lens:** Cache II/III scaffolding; OpArgs sealed-sum reservations; outbox helpers.

## Findings

- internal/cache/account.go:28-36 — 3 — `Cache` struct and `NewCache` constructor declared but never constructed outside the package; the App wires `*cache.Account` directly.
  Action: delete
  Rationale: deadcode confirms NewCache unreachable from main; grep confirms no external constructor; the `mu sync.RWMutex` field (golangci unused) dies with the type.

- internal/cache/account.go:29 — 3 — `mu sync.RWMutex` field on `Cache` never read or written after construction.
  Action: delete (subsumed by Cache deletion)
  Rationale: Cited separately because golangci flags it independently.

- internal/cache/account.go:279 — 3 — `errClosed` package-level var declared but never referenced.
  Action: delete
  Rationale: golangci unused + grep confirm zero call sites; scaffolded for a Close-guard never implemented.

- internal/cache/ops.go:34-43 — 3 — `SendArgs`, `AppendArgs`, `KindSend`, `KindAppend`, and their `opKind()` methods declared but have zero callers.
  Action: keep
  Rationale: Pass 8.4c (Cache III — outbox + offline) is a named scheduled pass with starter prompt in STATUS.md citing ADR-0117 (typed op events) as settled. SendArgs/AppendArgs are typed carriers for send/append ops Pass 9 (Compose) will enqueue. Valid speculative-consumer skip per the rationale guard.

- internal/cache/drainer.go:260-263 — 2 — `backoffFor` is a one-call-site wrapper that captures `cfg` and delegates verbatim to `backoff.Exponential`.
  Action: inline
  Rationale: Wrapper adds anonymous function allocation and capture for no ergonomic gain.

- internal/cache/account.go:179-192 — 7 — `AccountName()` and `AccountEmail()` are Middle Man proxies that delegate verbatim to `a.Backend.AccountName()` / `a.Backend.AccountEmail()` with only a nil-guard fallback.
  Action: keep
  Rationale: Nil-guard is load-bearing: `cmd/poplar/cache.go:258` opens an Account with nil backend for read-only CLI introspection (`evict --older-than`); `internal/ui/account_tab.go:79,777` calls both on `*cache.Account`.

- internal/cache/drainer.go:197-200 — 2 — `mark` is an unexported helper with two call sites, both within `finalizeSuccess` and both passing `OpDone`.
  Action: inline
  Rationale: Both call sites are `return mark(tx, row, OpDone)`; inlining one-line UPDATE removes indirection.

- internal/cache/account.go:243-255 — 2 — `expandHome` is called exactly once, from `DBPath`.
  Action: inline
  Rationale: DBPath already compact; inlining 8-line expandHome eliminates a named helper whose name adds no semantic value.
