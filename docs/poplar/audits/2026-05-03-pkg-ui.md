# Pass 8.5 B — Overengineering Audit: `internal/ui/`

**Date:** 2026-05-03
**Lens:** Overengineering + bubbletea-specific items (Elm conformance is Pass 8.5b).

## Findings

- internal/ui/help_popover.go:181 — 14 — `lipgloss.NewStyle()` called inside `HelpPopover.Box()`, outside the two permitted files.
  Action: refactor
  Rationale: Add a named `HelpBoxBorder lipgloss.Style` field to `Styles` (initialized in `NewStyles`); `Box()` reads it from `h.styles`.

- internal/ui/cmds.go:278 — 2 — `looksLikeRFC822` is unexported with one call site (line 229, inside `extractDisplayText`).
  Action: inline
  Rationale: 12-line predicate only used in one guard; inlining eliminates extract-method residue.

- internal/ui/cmds.go:228 — 2 — `extractDisplayText` is unexported with one call site (line 209 inside `loadBodyCmd`).
  Action: inline
  Rationale: Function names RFC822-walk logic; moving into the goroutine body of `loadBodyCmd` reduces indirection.

- internal/ui/confirm_modal.go:153 — 2 — `confirmWrap` is unexported with one call site (line 118 inside `ConfirmModal.Box()`).
  Action: inline
  Rationale: Two-argument Hardwrap/Wordwrap chain is one expression; named function adds a call frame with no reuse.

- internal/ui/app.go:416 — 2 — `translateConnState` is unexported with one call site (line 254 inside `App.Update`).
  Action: inline
  Rationale: Three-case switch with direct constant mapping; inlining avoids a scroll to find the translation.

- internal/ui/help_popover.go:224 — 2 — `HelpPopover.renderTopEdge` is unexported with one call site (line 188 inside `Box()`).
  Action: inline
  Rationale: Extract-method residue.

- internal/ui/viewer.go:360 — 2 — `viewerViewportKeymap` has one call site (line 307 inside `Viewer.layout()`).
  Action: inline
  Rationale: Configured once in `layout()`; named function adds ceremony without enabling reuse.

- internal/ui/linkpicker.go:275 — 2 — `parseLinkKey` has one call site (line 115 inside `LinkPicker.handleKey()`).
  Action: inline
  Rationale: Four-line digit-parse guard only evaluated in one place.
